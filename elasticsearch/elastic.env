# elastic.env
# Environment variables for Elasticsearch container.
# These control memory limits, security settings, and node discovery behavior.

# Set the JVM heap size
ES_JAVA_OPTS="-Xms1g -Xmx1g"

# Enable built-in security features (e.g. authentication, HTTPS)
xpack.security.enabled=true

# Run in single-node mode (useful for testing/labs)
discovery.type=single-node
